find the largest three numbers?
#include<stdio.h> 
int main()
{
    int num1,num2,num3,larger;
    printf("num1,num2,num3:");
    scanf("%d%d%d",&num1,&num2,&num3);
    if(num1>=num2&&num1>=num3)
    {
        larger=num1;
        printf("num1 is larger:");
    }
    else if(num2>=num1&&num2>=num3)
    {
        larger=num2;
        printf("num2 is larger:");
    }
    else{
        larger=3;
        printf("num3 is larger:");
    }
    printf("the larger number is:%d\n",larger);
    return 0;
********************************************************
OUTPUT
num1,num2,num3:10 30 20
num2 is larger:the larger number is:30
********************************************************
